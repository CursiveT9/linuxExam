{
  "dashboard": {
    "title": "Gateway Metrics",
    "panels": [
      {
        "type": "graph",
        "title": "Requests",
        "targets": [
          {
            "expr": "http_requests_total",
            "legendFormat": "{{method}} {{endpoint}}"
          }
        ]
      }
    ]
  }
}
